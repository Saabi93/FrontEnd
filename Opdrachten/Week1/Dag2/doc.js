 

let name = 'Mosh ';
console.log(name);
// Variables
//Cannot be a reserved keyword (let, if, else, var)
// Should be meaningful (not x)
// Cannot start with a number (1name)
// Cannot contain space or hyphen (-) 

let firstName = 'Lucas', lastName = 'Verberne'; //Camel notation
let firstName = 'Lucas'; 
let lastName = 'Verberne'; //Best practice

//Data types
//https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
// Dynamic typing
// let foo = 42;    // foo is now a number
// foo     = 'bar'; // foo is now a string
// foo     = true;  // foo is now a boolean

// Primitive data types (immutable):
// Boolean
// Null
// Undefined
// Number (2 built-in numeric types: Number and BigInt)
    // To check for the largest available value or smallest available value within ±Infinity, you can use the constants Number.MAX_VALUE or Number.MIN_VALUE.
    // you are also able to check if a number is in the double-precision floating-point number range using Number.isSafeInteger() as well as Number.MAX_SAFE_INTEGER and Number.MIN_SAFE_INTEGER.
    // BigInt (A BigInt is created by appending n to the end of an integer or by calling the constructor.)(Number.MAX_SAFE_INTEGER)
// String
// Symbol
// and Object

//Let erop dat = een compleet verschillende operator is dan == en ===. De = -operator is een zogehete "assignment" operator; hiermee ken je een waarde toe aan een variable. De == & === operators zijn beide "equals" operators; hiermee controleer je of twee values gelijk zijn aan elkaar.
